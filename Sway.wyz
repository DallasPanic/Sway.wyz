local Http = game:GetService("HttpService")
local MyKey = getgenv().Key
if not MyKey then error("set key") end
local KeyUrl = "https://raw.githubusercontent.com/DallasPanic/Sway.wyz/main/Key"
local success, ServerKey = pcall(function()
    return game:HttpGet(KeyUrl)
end)
if not success then error("no internet") end
if MyKey ~= ServerKey:gsub("%s","") then error("bad key") end
print("hi")
